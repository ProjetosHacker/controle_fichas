 DROP TABLE FICHA;
CREATE TABLE FICHA (
	NUMFICHA INTEGER NOT NULL AUTO_INCREMENT,
	MATRICULA NUMERIC(12,0),
	NOMESERVIDOR VARCHAR(60),
	NOMEMAE VARCHAR(60),
	DTNASC DATE,
	CPF VARCHAR(14),
	CODLOCAL INTEGER,
	ESTANTE INTEGER,
	PRATELEIRA INTEGER,
	SITFICHA CHAR(3),
	CODUSUEMP INTEGER,
	RG NUMERIC(15,0),
	ORGAOEXP CHAR(3),
	UF CHAR(2),
	PRIMARY KEY (NUMFICHA)
);

SELECT * FROM FICHA;

-- Ajustar o CPF PARA TIRAR OS PONTOS
SET SQL_SAFE_UPDATES=0;
UPDATE FICHA_2 SET CPF = REPLACE( REPLACE( CPF, '.', '' ), '-', '' );